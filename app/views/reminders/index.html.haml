%p#notice= notice
%h1 Lembretes
%table
  %thead
    %tr
      %th Name
      %th Time
      %th Days
      %th Start at
      %th{:colspan => "3"}
  %tbody
    - @reminders.each do |reminder|
      %tr
        %td= reminder.name
        %td= reminder.time
        %td= reminder.days
        %td= reminder.start_at
        %td= link_to 'Show', reminder
        %td= link_to 'Edit', edit_reminder_path(reminder)
        %td= link_to 'Destroy', reminder, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'Novo Lembrete', new_reminder_path
